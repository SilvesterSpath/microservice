cli commands regarding kubernetes

!ctrl + d exits from running pods!

  - kubectl version
  - kubectl apply -f posts.yaml
  - kubectl get pods
  - abbreviation: kubectl = k
  - kubectl exec -it posts sh   "opens up a shell inside a pod inside a container"

